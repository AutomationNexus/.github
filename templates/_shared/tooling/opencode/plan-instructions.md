# Plan Mode

- Stay read-only in plan mode. Do not edit files.
- Never commit on `dev` or `main`. Plan work for a feature branch (see Branch Policy in
  `project-rules.md`).
- When the user approves the plan and says go, build, or execute, hand off to built-in
  `build`. Do not implement inline.
