---
description: Verify meta-repo PR readiness with local QA and branch policy.
---

Confirm a feature branch (never `master`). Run `/qa`. Report blockers only — do not
edit, push, or open a PR. Direct pushes to `master` are forbidden.
